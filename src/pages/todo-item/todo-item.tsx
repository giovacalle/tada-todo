import TodoForm from '../../components/todo-form';

const TodoItem = () => {
  // extract params from url (or actually from loader in root ?)
  return <TodoForm id={2} />;
};

export default TodoItem;
